services:
  - type: web
    name: bridge-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn bridge_buyer_backend:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        fromSecret: DATABASE_URL
