# docker-compose.yml (BRidge — api + worker + db)

x-env: &default-env
  ENV: "production"
  PYTHONUNBUFFERED: "1"
  UVICORN_WORKERS: "1"

services:
  db:
    image: postgres:15
    container_name: bridge_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-postgres}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 10
    volumes:
      - db_data:/var/lib/postgresql/data
    ports:
      - "5433:5432"

  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bridge_api
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    env_file: .env
    environment:
      <<: *default-env
      ALLOW_LOCALHOST_IN_PROD: "1"
      ALLOW_PUBLIC_SELLER_SIGNUP: "1"
      DATABASE_URL: "postgresql://postgres:${POSTGRES_PASSWORD:-postgres}@db:5432/${POSTGRES_DB:-postgres}"
      GUNICORN_CMD_ARGS: "--timeout 90"
      BRIDGE_BOOTSTRAP_DDL: "0"
    command: >
      gunicorn bridge_buyer_backend:app
      -k uvicorn.workers.UvicornWorker
      -w ${API_WORKERS:-1}
      --timeout 90
      --bind 0.0.0.0:8000
    ports:
      - "8000:8000"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:8000/healthz || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 5
    volumes:
      - ./static:/app/static:ro
      - ./tests:/app/tests:ro

  worker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bridge_worker
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    env_file: .env
    environment:
      <<: *default-env
      ALLOW_LOCALHOST_IN_PROD: "1"
      DATABASE_URL: "postgresql://postgres:${POSTGRES_PASSWORD:-postgres}@db:5432/${POSTGRES_DB:-postgres}"
      BRIDGE_BOOTSTRAP_DDL: "0"
    command: ["python","-u","worker.py"]

volumes:
  db_data:
