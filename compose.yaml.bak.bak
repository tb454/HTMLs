```yaml
services:
  app:
    build:
      context: ./
    ports:
      - "3000:3000"
    restart: unless-stopped
    init: true
    depends_on:
      - backend
  backend:
    build:
      context: ./backend
    ports:
      - "8000:8000"
    restart: unless-stopped
    init: true
networks:
  default:
    driver: bridge
```